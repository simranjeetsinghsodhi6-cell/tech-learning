import type { Metadata } from 'next';
import Link from 'next/link';
import { courses } from '@/lib/courses';
export const metadata: Metadata = { title: 'Dashboard' };
export default function DashboardPage() { return <main className="mx-auto max-w-7xl px-4 py-16"><h1 className="text-4xl font-black md:text-6xl">Dashboard</h1><p className="mt-4 max-w-2xl text-muted-foreground">Track active courses, plan weekly progress, and continue your next production milestone.</p><div className="mt-10 grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><aside className="rounded-3xl border border-border bg-card p-6"><h2 className="text-2xl font-black">This week</h2><p className="mt-3 text-muted-foreground">Choose a course and commit to one module. Your persisted progress can be connected to Supabase profile tables.</p><Link className="mt-6 inline-flex rounded-full bg-primary px-5 py-3 font-bold text-primary-foreground" href="/courses">Browse courses</Link></aside><section className="grid gap-4 md:grid-cols-2">{courses.slice(0,2).map((course)=><Link className="rounded-3xl border border-border bg-card p-6" href={`/courses/${course.slug}`} key={course.slug}><p className="font-bold text-primary">Continue</p><h3 className="mt-2 text-xl font-black">{course.title}</h3><p className="mt-2 text-sm text-muted-foreground">{course.modules[0]}</p></Link>)}</section></div></main>; }
