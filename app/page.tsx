import Link from 'next/link';
import { ArrowRight, CheckCircle2 } from 'lucide-react';
import { courses } from '@/lib/courses';
export default function Home() { return <main><section className="mx-auto grid max-w-7xl gap-10 px-4 py-20 lg:grid-cols-[1.1fr_.9fr] lg:py-28"><div><p className="font-bold uppercase tracking-[0.25em] text-primary">Project-based learning platform</p><h1 className="mt-5 text-5xl font-black tracking-tight md:text-7xl">Learn the stack teams use to ship modern web products.</h1><p className="mt-6 max-w-2xl text-lg text-muted-foreground">Tech Learning combines structured courses, authentication-ready workspaces, profile management, and deployment practices for Next.js, Tailwind CSS, TypeScript, Supabase, GitHub, and Vercel.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link className="inline-flex items-center justify-center rounded-full bg-primary px-6 py-3 font-bold text-primary-foreground" href="/courses">Explore courses <ArrowRight className="ml-2 h-5 w-5" /></Link><Link className="inline-flex items-center justify-center rounded-full border border-border px-6 py-3 font-bold" href="/signup">Create account</Link></div></div><div className="rounded-[2rem] border border-border bg-card/80 p-6 shadow-soft"><p className="text-sm font-bold uppercase tracking-widest text-primary">Platform readiness</p>{['Responsive layouts','SEO metadata','Dark and light mode','Supabase authentication','Vercel-ready build'].map((item)=><div className="mt-5 flex items-center gap-3" key={item}><CheckCircle2 className="h-5 w-5 text-primary"/><span className="font-semibold">{item}</span></div>)}</div></section><section className="mx-auto max-w-7xl px-4 pb-20"><div className="mb-8 flex items-end justify-between gap-4"><div><p className="font-bold uppercase tracking-widest text-primary">Featured courses</p><h2 className="mt-2 text-3xl font-black md:text-5xl">Start with a guided path.</h2></div><Link className="font-bold text-primary" href="/courses">View all</Link></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-4">{courses.map((course)=><Link className="rounded-3xl border border-border bg-card p-6 transition hover:-translate-y-1 hover:shadow-soft" href={`/courses/${course.slug}`} key={course.slug}><p className="text-sm font-bold text-primary">{course.level} • {course.duration}</p><h3 className="mt-4 text-xl font-black">{course.title}</h3><p className="mt-3 text-sm text-muted-foreground">{course.description}</p></Link>)}</div></section></main>; }
