import Link from 'next/link';
import { ThemeToggle } from './theme-toggle';
const nav = [{ href: '/courses', label: 'Courses' }, { href: '/dashboard', label: 'Dashboard' }, { href: '/profile', label: 'Profile' }];
export function Header() { return <header className="sticky top-0 z-50 border-b border-border bg-background/80 backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/" className="flex items-center gap-3 font-black"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-primary text-primary-foreground">TL</span>Tech Learning</Link><div className="hidden items-center gap-6 text-sm font-semibold text-muted-foreground md:flex">{nav.map((item)=><Link className="hover:text-foreground" href={item.href} key={item.href}>{item.label}</Link>)}</div><div className="flex items-center gap-3"><ThemeToggle/><Link className="rounded-full bg-primary px-4 py-2 text-sm font-bold text-primary-foreground" href="/login">Sign in</Link></div></nav></header>; }
